import { ChatInterface } from '@/components/chat/chat-interface';

export default function Home() {
  return (
    <main className="h-screen w-full bg-slate-100 overflow-hidden">
      <ChatInterface />
    </main>
  );
}